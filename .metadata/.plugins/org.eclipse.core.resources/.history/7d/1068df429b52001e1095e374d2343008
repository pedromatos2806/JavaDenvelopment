
public class ContaPoupanca extends ContaBancaria{
	private double diaRendimento;
	
	public ContaPoupanca (String nomeCliente, String numConta, double saldo,double diaRendimento ) {
		super(nomeCliente,numConta,saldo);
		this.diaRendimento = diaRendimento;
	}
	
	public void calcularNovoSaldo(double saldo) {
		 double novosaldo = getSaldo() * ((diaRendimento/100)+1);
		 setSaldo(novosaldo);
	}
	
	@Override
	public String toString() {
		return Double.toString(getSaldo());
	}
}