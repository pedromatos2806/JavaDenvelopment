
public class ContaEspecial  extends ContaBancaria{
	private double limite; 
	
	public ContaEspecial(String nomeCliente, String numConta, double saldo, double limite) {
		super(nomeCliente, numConta, saldo);
		this.limite = limite;
		// TODO Auto-generated constructor stub
	}
	
	@Override
	public void Sacar(double value) throws Exception{
		if(value <= super.getSaldo() + limite) {
			super.Sacar(value);
		}else {
			throw new Exception() ;
		}
	}

	//Pq não pode por o Override aqui?
	@Override
	public String toString() {
		String saldo = Double.toString(super.getSaldo());
		return saldo;
	}
}
